@model EzyPeezy.Models.Transaction

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Backend.cshtml";
}



<div class="content-wrapper">
    <div class="row">

        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">

                    <p>


                        <h4>Are you sure you want to delete this sale transaction?</h4>
                    </p>
                    <br />
                    <div>
                        <dl class="dl-horizontal">
                            <dt>
                                @Html.DisplayNameFor(model => model.TransactionDateTime)
                            </dt>

                            <dd>
                                @Html.DisplayFor(model => model.TransactionDateTime)
                            </dd>

                            <dt>
                                @Html.DisplayNameFor(model => model.Amount)
                            </dt>

                            <dd>
                                @Html.DisplayFor(model => model.Amount)
                            </dd>

                        </dl>

                        @using (Html.BeginForm())
                        {
                          @Html.AntiForgeryToken()

                                    <div class="col-lg-10 col-lg-offset-2">
                                        <a href="@Url.Action("Index")" class="btn btn-default">Cancel</a>
                                        <input type="submit" value="Delete" class="btn btn-danger" />
                                    </div>}
                    </div>
                </div>
            </div>
        </div>


    </div>
</div>